# Local development uses SQLite. In production (Render), point this at a Postgres
# instance — Render's disk is ephemeral and a SQLite file would be lost on redeploy.
DATABASE_URL=sqlite:///./typeform.db
CORS_ORIGINS=http://localhost:3000
FRONTEND_URL=http://localhost:3000
SEED_ON_STARTUP=true
