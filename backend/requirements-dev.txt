# Test-only dependencies. Kept separate from requirements.txt so the deployed
# service does not install anything it never imports at runtime.
-r requirements.txt

# Starlette's TestClient is a thin wrapper over httpx, so smoke_test.py needs it.
# Pinned below 0.28 for compatibility with the Starlette version FastAPI 0.115.6 requires.
httpx==0.27.2
